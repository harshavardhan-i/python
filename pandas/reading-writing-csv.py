# CSV - comma seperated values
# It stores text data in a tabular format
# Each row of data has it's own line and each value is seperated by a comma 
# Most databases and spreadsheets program can use them or create them

# pd.read_csv("file name") - reads and converts the csv into a dataframe
# df.to_csv("file name") - converts the dataframe into a CSV file
