# Bar Graph…
# Plt.bar(x, y)…. Plots bar graph, by default vertical bars
# Plt.ylabel("")…. There is a x label defined already usually
# Plt.barh(x, y)…. Shows horizontal bars, only need to define plt.ylabel() again
# Plt.bar(x, y, yerr=df.error)…. Used to display the error …
# Plt.bar(x, y1, bottom=y1)… In a stacked bar situation y2 would start on top of y1


# Histograms…
# Histogram visualized which data points fall into which buckets
# Plt.hist(data)… only takes one parameter to draw the visuals
# Plt.hist(data, bin=90)… Fixes the no. of buckets in the visuals
# Plt.hist( data, range=(xmin, xmax))… decides the max range of data to be displayed on the graph
# Normalizing a histogram???....
# Plt.his( data, density=True)…normalizes the data points
